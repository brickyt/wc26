[build]
  publish = "public"
  functions = "netlify/functions"

# Optional friendly alias -> /api/scores hits the function
[[redirects]]
  from = "/api/scores"
  to = "/.netlify/functions/scores"
  status = 200
